# TODO

A short index of what could come next.
Every item is summarized in one line here and argued in [README.md](README.md); follow the link before acting on one.

## Open items

- [ ] Consider copier, which would ask questions when creating a project and carry later fixes into it ([consider copier](README.md#consider-copier))
- [ ] Decide whether the anchor check belongs in every project ([the anchor check](README.md#decide-whether-the-anchor-check-belongs-in-every-project))
- [ ] Decide what goes in a global excludes file and what stays in each project's `.gitignore` ([global excludes](README.md#split-global-excludes-from-per-project-ignores))
- [ ] Decide whether copying should run `git init` and `pre-commit install`, or only print them ([copy steps](README.md#decide-whether-copying-runs-git-init-and-pre-commit-install))
