# The environment variables this project reads, by name and without values.
#
# Copy this file to .env and fill in the values there. .env is ignored by git;
# this file is committed, so never put a real value in it.
#
# List one variable per line, with a comment saying what it is for:
#
#   # Key for the example API.
#   EXAMPLE_API_KEY=
